import { ScrollbarElementProps } from '@local_modules/Scrollbar/Scrollbar.type';
import { ScrollView } from 'react-native';

export default function NativeScrollbar(props: ScrollbarElementProps) {
  return (
    <ScrollView></ScrollView>
  )
}